# appxserver-portable

Symlink or copy the full package from:

`myappx-server/images/myappxserver/appxserver-portable`

This directory is used by `scripts/beforepack.js` when building the desktop installer.
